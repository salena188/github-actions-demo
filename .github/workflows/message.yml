name: Print Multiple Message

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Multiple Message
        run: echo "Welcome Students"
              echo "Learning GitHub Actions"
              echo "Today's Lab Session"
